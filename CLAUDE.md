# claude-copet — project rules

## Tooling / skills
- **Do not read files from other projects** (e.g. the `awesome-agentic-things` /
  `weee-skills` plugin source). Those skills (`weee:dev`, `weee:tdd`,
  `weee:verify`, etc.) are already installed — invoke and follow the loaded skill
  instructions directly; never open their source files to inspect prompts or
  templates. Stay within this repo (`/Users/dengwu.wang/Study/claude-copet`).
